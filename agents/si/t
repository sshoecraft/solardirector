include("../../core/utils.js");
influx.database = 'power';
dest = new Influx();
dest.database = "power";
var arr = influx2arr(influx.query("select * from si_efficiency"),true);
dprintf(0,"len: %d\n", arr.length);
if (arr.length) dest.query("delete from si_deff");
for(i=0; i < arr.length; i++) {
//	dumpobj(arr[i]);
	let nobj = {};
	nobj.time = arr[i].time;
	nobj.value = arr[i].efficiency;
	dest.write("si_deff",nobj);
}
//influx.query("drop measurement si_efficiency");
