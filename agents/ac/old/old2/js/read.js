
function read_main() {

return;
	let dlevel = 1;

	run(script_dir+"/temp.js");
	run(script_dir+"/mode.js");
	run(script_dir+"/cycle.js");
	run(script_dir+"/sample.js");
	run(script_dir+"/charge.js");
}
